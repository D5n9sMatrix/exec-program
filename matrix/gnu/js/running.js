/*You cannot perform other kinds of operations on entire arrays by single statements — in most Rexx
implementations. For example, these statements are invalid and result in errors:*/
(function(array){

    var a = array.length[max.indexOf(2)];   /** a array  */
    var m = array.length[max.indexOf(3)];   /** m array */
    var p = array.length[max.indexOf(4)];   /** p array */
    var t = array.length[max.indexOf(5)];   /** t array */
    var l = array.length[max.indexOf(6)];   /** l array */

    return array[a, m, p, t, l];

})
